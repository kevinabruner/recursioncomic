- name: Re-deploy dev servers for fresh installation 
  hosts: recursioncomic:&env_dev
  vars_files:
    - vars.yaml
    - vault.yaml
  serial: 1  
  tasks:
    - name: Wipe dev tasks from ansible 
      import_tasks: /home/{{ ANSIBLE_USER }}/ansible/playbook/tasks/drupal/wipe-dev.yaml